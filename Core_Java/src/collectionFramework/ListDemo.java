package collectionFramework;

public class ListDemo {

	/*
	 Enumeration is the cursor which is used to retrieve collection
	 objects one by one.
	 
	 It was introduced in JDK 1.0
	 
	 It can only be used for Legacy Class. i.e Vector and Stack
	 
	 Enumeration cursor can be get by elements() method
	 
	 Method of Enumeration cursor are :- 
	 hasMoreElements(), nextElement()
	 
	 It can be used to retirve only in forward direction.
	 
	 It can only be used for read operation
	 
	 
	 
	 */
}
